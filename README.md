# wikitable-scraper
A script for scraping Wikipedia tables and saving them as .csv files. 

# Usage
Replace the value of the ``url`` variable with a Wikipedia URL of your choice and run ``python wikitable-scraper.py``. Each table will be saved as a .csv file.

# Limitations
The script does not recognize tables that do not have the ``wikitable`` class.
